---
/** Simple flex grid wrapper for Card components. */
---

<div class="opuscor-card-grid"><slot /></div>

<style>
  .opuscor-card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: var(--opuscor-s-5);
  }
</style>
