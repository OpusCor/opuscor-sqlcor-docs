---
/**
 * Sidebar sublist override — passes through to Starlight default
 * (no per-item UK badge; language is chosen via the header dropdown).
 */
import Default from '@astrojs/starlight/components/SidebarSublist.astro';

const { sublist, nested } = Astro.props;
---

<Default sublist={sublist} nested={nested} />
